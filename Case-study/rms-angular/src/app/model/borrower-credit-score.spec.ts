import { BorrowerCreditScore } from './borrower-credit-score';

describe('BorrowerCreditScore', () => {
  it('should create an instance', () => {
    expect(new BorrowerCreditScore()).toBeTruthy();
  });
});
